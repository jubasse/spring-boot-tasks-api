# Each cached test context keeps its own Testcontainers running (Postgres, RabbitMQ, Mailpit, RustFS, ClamAV).
# Spring's default of 32 cached contexts kept too many of them alive at once: an evicted context is closed and
# stops its containers.
spring.test.context.cache.maxSize=8
